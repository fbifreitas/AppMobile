class AssistiveVisualClassificationPlaceholderService {
  const AssistiveVisualClassificationPlaceholderService();

  bool get isVisualAiEnabled => false;

  String get statusMessage =>
      'IA visual futura ainda não está habilitada neste build. A camada atual prepara a estrutura para classificação assistida no futuro.';
}
